name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Start Python's werkzeug 
      run: cd WikfixResume | python -m SimpleHTTPServer 80
